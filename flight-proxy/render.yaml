services:
  - type: web
    name: aerosync-flight-proxy
    env: node
    rootDir: flight-proxy
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: AIRLABS_API_KEY
        sync: false
      - key: CONCIERGE_AI_ENABLED
        value: "false"
